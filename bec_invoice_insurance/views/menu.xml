<?xml version="1.0" encoding="UTF-8"?>
<odoo>

	<record id="act_invoice_insurance" model="ir.actions.act_window">
        <field name="name">Invoice Insurance</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">invoice.insurance</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new invoice insurance
          </p>
        </field>
	</record>

	<menuitem id="menu_invoice_insurance"
	name="Invoice Insurance"
	parent="account.menu_finance_receivables"
	sequence='1'
	action="bec_invoice_insurance.act_invoice_insurance"/>

   
</odoo>

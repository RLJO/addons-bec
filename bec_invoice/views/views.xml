<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="bec_invoice.list">
      <field name="name">bec_invoice list</field>
      <field name="model">bec_invoice.bec_invoice</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="bec_invoice.action_window">
      <field name="name">bec_invoice window</field>
      <field name="res_model">bec_invoice.bec_invoice</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="bec_invoice.action_server">
      <field name="name">bec_invoice server</field>
      <field name="model_id" ref="model_bec_invoice_bec_invoice"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="bec_invoice" id="bec_invoice.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="bec_invoice.menu_1" parent="bec_invoice.menu_root"/>
    <menuitem name="Menu 2" id="bec_invoice.menu_2" parent="bec_invoice.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="bec_invoice.menu_1_list" parent="bec_invoice.menu_1"
              action="bec_invoice.action_window"/>
    <menuitem name="Server to list" id="bec_invoice" parent="bec_invoice.menu_2"
              action="bec_invoice.action_server"/>
    -->
  </data>
</odoo>